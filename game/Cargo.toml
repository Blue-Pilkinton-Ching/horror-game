[package]
name = "game"
version = "0.1.0"
edition = "2024"


[dependencies]
# Could potentially use webgpu feature in the future
bevy = {version = "0.18.1", default-features = false, features = ["3d","ui", "webgpu"]} 
noise = "0.9.0"
